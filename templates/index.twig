<!DOCTYPE html>
<html lang="en-US">
<head>
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta charset="utf-8" />
    <title>Rocket Media Coding Activity</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover" />
    <meta name="referrer" content="origin-when-cross-origin" />
    <link href="https://unpkg.com/tailwindcss@^1.0/dist/tailwind.min.css" rel="stylesheet">
    <link href="{{ siteUrl }}/assets/styles.css" rel="stylesheet">
</head>

<body class="box-border">
    <main class="container max-w-screen-sm p-5 sm:p-6">
        <header>
            <div class="inline-block py-1 px-3 bg-blue-700 text-xs uppercase text-white">Process</div>
            <h1 class="font-bold text-4xl my-5 leading-tight">{{ entry.mainTitle }}</h1>
        </header>
        <section>
            <!-- Step #1 -->
            <article class="block mb-8 sm:mb-5 sm:clear-both">
                <div class="step-wrap flex-wrap relative block sm:inline-block">
                    <div class="post-number text-white font-bold text-xl">1</div>
                    <div class="w-10/12">
                        <img src="{{ entry.step1ImageUrl }}" alt="{{ entry.step1Title }}">
                    </div>
                </div>
                <div class="flex-wrap block w-full sm:inline-block sm:w-3/5 sm:float-right">
                    <h2 class="font-bold text-2xl my-2 leading-tight sm:text-3xl sm:mb-2 sm:mt-0">{{ entry.step1Title }}</h2>
                    <p class="post-details">{{ entry.step1Details }}</p>
                </div>
            </article>
            <!-- Step #2 -->
            {% if entry.step2Title|length > 1 %}
            <article class="block mb-8 sm:pt-5 sm:clear-both">
                <div class="step-wrap flex-wrap relative block sm:inline-block">
                    <div class="post-number text-white font-bold text-xl">2</div>
                    <div class="w-10/12">
                        <img src="{{ entry.step2ImageUrl }}" alt="{{ entry.step2Title }}">
                    </div>
                </div>
                <div class="flex-wrap block w-full sm:inline-block sm:w-3/5 sm:float-right">
                    <h2 class="font-bold text-2xl my-2 leading-tight sm:text-3xl sm:mb-2 sm:mt-0">{{ entry.step2Title }}</h2>
                    <p class="post-details">{{ entry.step2Details }}</p>
                </div>
            </article>
            {% endif %}
            <!-- Step #3 -->
            {% if entry.step3Title|length > 1 %}
            <article class="block mb-8 sm:pt-5 sm:clear-both">
                <div class="step-wrap flex-wrap relative block sm:inline-block">
                    <div class="post-number text-white font-bold text-xl">3</div>
                    <div class="w-10/12">
                        <img src="{{ entry.step3ImageUrl }}" alt="{{  entry.step3Title }}">
                    </div>
                </div>
                <div class="flex-wrap block w-full sm:inline-block sm:w-3/5 sm:float-right">
                    <h2 class="font-bold text-2xl my-2 leading-tight sm:text-3xl sm:mb-2 sm:mt-0">{{  entry.step3Title }}</h2>
                    <p class="post-details">{{ entry.step3Details }}</p>
                </div>
            </article>
            {% endif %}
            <!-- Step #4 -->
            {% if entry.step4Title|length > 1 %}
            <article class="block mb-8 sm:py-5 sm:clear-both">
                <div class="step-wrap flex-wrap relative block sm:inline-block">
                    <div class="post-number text-white font-bold text-xl">4</div>
                    <div class="w-10/12">
                        <img src="{{ entry.step4ImageUrl }}" alt="{{ entry.step4Title }}">
                    </div>
                </div>
                <div class="flex-wrap block w-full sm:inline-block sm:w-3/5 sm:float-right">
                    <h2 class="font-bold text-2xl my-2 leading-tight sm:text-3xl sm:mb-2 sm:mt-0">{{ entry.step4Title }}</h2>
                    <p class="post-details">{{ entry.step4Details }}</p>
                </div>
            </article>
            {% endif %}
        </section>
    </main>
</body>
</html>
